Received the manifest-approved sequential structures from demo-algorithms: 9 demos, 12 conformance files (21 native tests/cases), shared src definitions, and the updated dynamic-sequence report. Added a migration regression contract that checks catalog counts, file presence, include paths, and forbids include in any future web/*.mlpl. Fixed target runners to set the repository source root and delegate explicit catalog paths to mlplunit. Full just check passed. Implementation commit: 9a656c7.