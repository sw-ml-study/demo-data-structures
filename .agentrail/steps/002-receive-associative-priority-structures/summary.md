Received 9 associative/priority/cache demos and 9 mlplunit conformance files (9 native tests) with shared src definitions and the modern hashing assessment. Heap sort remains standalone and algorithm-owned in demo-algorithms. Added a failing-then-passing migration contract and routine web standalone audit forbidding include under web/*.mlpl. Full just check passed: 18 demos and 30 native tests/cases. Implementation commit f42d106.